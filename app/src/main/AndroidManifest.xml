<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ucsdschedulinghelper" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.ucsdschedulinghelper.MajorChoiceActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.ucsdschedulinghelper.ui.courseListView.ListViewLoader"
            android:label="ListViewLoader">
        </activity>

        <provider
            android:name=".provider.CoursesContentProvider"
            android:authorities="com.example.ucsdschedulinghelper.provider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:enabled="true">
        </provider>

    </application>

</manifest>
